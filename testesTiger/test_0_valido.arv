 exp(
  exp(
   exp(
    exp(
     iffExp(
      exp(
       exp(
        exp(
         exp(
          seqExp(
           expList(
            exp(
             exp(
              opExp(
               GREAT,
               exp(
                exp(
                 intExp(5))),
               exp(
                exp(
                 intExp(4)))))),
            expList())))))),
      exp(
       exp(
        exp(
         exp(
          intExp(13))))),
      exp(
       exp(
        exp(
         exp(
          intExp(14))))))))))